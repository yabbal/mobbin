---
title: Collections
description: Access the user's saved collections.
---

## CollectionsResource

Collections are groups of screens saved by the user via the Mobbin web interface.

### list

```typescript
const collections = await client.collections.list();

for (const collection of collections) {
  console.log(`${collection.name} (${collection.id})`);
}
```
